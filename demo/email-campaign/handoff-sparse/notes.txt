The real customer export is always in Downloads with a random name — import whatever is newest.

Unsubscribe link: we will add the default footer later.

If anyone asks about GDPR just say we are still a small team.
